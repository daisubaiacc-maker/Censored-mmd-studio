# Mobile UI

Dedicated mobile/touch UI layer.

This layer is intentionally empty of desktop markup for now. The mobile UI will be designed separately after the UI boundary is established.

Planned responsibilities:
- Touch-oriented controls
- Camera / Model / Region operation modes
- Mobile zoom control
- Large touch targets
- Mobile-specific layout and interaction feedback

Do not solve mobile UI by shrinking the PC UI with CSS.
