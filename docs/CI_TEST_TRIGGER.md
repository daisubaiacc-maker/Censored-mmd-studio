# CI test trigger

This file exists only to trigger the feature-branch Build and Pages deployment workflows after the deployment environment rule was changed.

It can be removed after the CI/Pages pipeline is confirmed working.
